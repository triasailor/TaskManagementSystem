package com.myreminder.myreminder;

/**
 * The {@code ReminderCat} enum provides the enumeration of the reminder categories,
 * and hence validating the total possible category names of a Reminder object.
 */
public enum ReminderCat {
    ONE_DAY_BEFORE,ONE_WEEK_BEFORE,ONE_MONTH_BEFORE,SPECIFIC_DATE_BEFORE
}
